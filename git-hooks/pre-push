#!/usr/bin/env bash
set -euo pipefail

# Check Python file size limits for files changed in commits being pushed.
# This prevents legacy oversized files from blocking unrelated pushes.
tracked_py_files=""
upstream_ref="$(git rev-parse --abbrev-ref --symbolic-full-name @{upstream} 2>/dev/null || true)"
if [[ -n "${upstream_ref}" ]]; then
  merge_base="$(git merge-base HEAD "${upstream_ref}" 2>/dev/null || true)"
  if [[ -n "${merge_base}" ]]; then
    tracked_py_files="$(git diff --name-only --diff-filter=AM "${merge_base}"...HEAD -- '*.py' || true)"
  fi
fi
if [[ -z "${tracked_py_files}" ]]; then
  tracked_py_files="$(git diff --name-only --diff-filter=AM HEAD~1..HEAD -- '*.py' || true)"
fi
line_count_fail=0
if [[ -n "${tracked_py_files}" ]]; then
  while IFS= read -r file; do
    if [[ -f "${file}" ]]; then
      line_count=$(wc -l < "${file}" | tr -d ' ')
      if (( line_count > 1000 )); then
        echo "ERROR: ${file} has ${line_count} lines (limit is 1000)." >&2
        line_count_fail=1
      elif (( line_count > 700 )); then
        echo "WARNING: ${file} has ${line_count} lines (warning at 700)." >&2
      fi
    fi
  done <<< "${tracked_py_files}"
fi

if (( line_count_fail != 0 )); then
  exit 1
fi

changed_py=()
if [[ -n "${tracked_py_files}" ]]; then
  while IFS= read -r file; do
    [[ -n "${file}" ]] && changed_py+=("${file}")
  done <<< "${tracked_py_files}"
fi

PYTHON_BIN="python3"
if [[ -x ".venv/bin/python" ]]; then
  PYTHON_BIN=".venv/bin/python"
fi
BASEDPYRIGHT_BIN="basedpyright"
if [[ -x ".venv/bin/basedpyright" ]]; then
  BASEDPYRIGHT_BIN=".venv/bin/basedpyright"
fi

if (( ${#changed_py[@]} > 0 )); then
  echo "Running black --check on changed Python files..."
  ${PYTHON_BIN} -m black --check "${changed_py[@]}"

  echo "Running ruff on changed Python files..."
  ${PYTHON_BIN} -m ruff check "${changed_py[@]}"

  echo "Running basedpyright on changed Python files..."
  ${BASEDPYRIGHT_BIN} --pythonpath ${PYTHON_BIN} "${changed_py[@]}"
else
  echo "No changed Python files for black/ruff/basedpyright."
fi

echo "Running full test suite with coverage (min 70%)..."
${PYTHON_BIN} -m pytest tests/ -v --cov=src --cov-report=term-missing --cov-fail-under=70
